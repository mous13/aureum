{% block header %}
    <link href="https://fonts.cdnfonts.com/css/sageffine" rel="stylesheet">
    <header class="header">
        <div class="responsive flex">
            {% block header_logo %}
                <a href="{{ path('forumify_core_index') }}" class="header-logo">
                    <img src="{{ logo }}" alt="logo">
                    <span class="ml-2 text-capitalize">{{ setting('forumify.title') }}</span>
                </a>
            {% endblock %}
            {% block header_navigtion %}
                <div class="flex flex-grow justify-end items-center">
                    {% include '@Forumify/frontend/blocks/header/menu_large.html.twig' %}
                    {% include '@Forumify/frontend/blocks/header/menu_mobile.html.twig' %}
                    {% if app.user is not null %}
                        {{ component('Notifications') }}
                    {% endif %}
                </div>
            {% endblock %}
        </div>
    </header>
    <hr>
{% endblock %}