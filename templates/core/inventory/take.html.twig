{% extends '@CitadelAureum/core/base.html.twig' %}

{% block title %}Stock take{% endblock %}

{% block body %}
    <div class="mb-4 flex flex-wrap items-center justify-between">
        <h1 class="mb-0">{{ inventory.name }}</h1>
        <a class="btn btn-link" href="{{ path('aureum_inventory') }}">Back to Inventory</a>
    </div>

    {{ component('Aureum\\StockTake', { inventoryId: inventory.id }) }}
{% endblock %}
