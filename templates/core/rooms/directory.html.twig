{% extends '@CitadelAureum/core/base.html.twig' %}
{% block body %}
    {{ component('Aureum\\Announcements', { module: 'rooms' }) }}
    <div class="card">
        <div class="card-title luxury">
            Rooms Directory
        </div>
        <div class="card-body">
            {{ component('RoomsDirectory', { hotelId: hotelId }) }}
        </div>
    </div>
{% endblock %}